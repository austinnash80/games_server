Welcome to Games Sherpa.

<% if @current_user.present? && @current_user.player == true %>
  <%= link_to "Events", events_path, :class => 'navbar-link' %>
  <%= link_to "Games", games_path, :class => 'navbar-link' %>
<% end %>

<% if @current_user.present? && @current_user.sherpa == true %>
  <%= link_to "Edit Profile", edit_user_path(@user), :class => 'navbar-link' %>
  <%= link_to "Games", games_path, :class => 'navbar-link' %>
  <%= link_to "Events", events_path, :class => 'navbar-link' %>
<% end %>

<% if @current_user.present? && @displayed_msg_flag != true %>
    Welcome back <%= @current_user.name || @current_user.email%>
    <% @displayed_msg_flag = true%>
<% end %>
